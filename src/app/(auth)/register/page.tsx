import { RegisterSwitch } from "@/features/auth/components/RegisterSwitch";

export const metadata = { title: "नोंदणी | महसूल संकेत" };
export const dynamic = "force-dynamic";

export default function RegisterPage() {
  return <RegisterSwitch />;
}
